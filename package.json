{
  "name": "limeHome-Challenge",
  "version": "0.0.0",
  "scripts": {
    "build": "cd front-end && npm install && node scripts/create-env-variable.js && npm run build && cd ../back-end && npm install",
    "start": "cd back-end && npm start",
    "heroku-postbuild": "npm run build"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~3.7.5"
  },
  "dependencies": {},
  "engines": {
    "node": "12.16.1",
    "npm": "6.13.4"
  }
}